# PyTorch 源（版本不锁死，按 Isaac Gym 兼容性解析）
-f https://download.pytorch.org/whl/torch_stable.html
torch
torchvision

# MuJoCo Python 绑定（示例兼容版本）
mujoco-py==2.1.2.14

# IsaacGymEnvs 运行依赖（与 IsaacGymEnvs/setup.py 对齐）
gym==0.23.1
omegaconf==2.3.0
termcolor==2.4.0
jinja2==3.1.6
hydra-core==1.3.2
# rl-games 版本由项目需求决定；如与 torch 冲突可按构建日志回退
rl-games==1.6.1
pyvirtualdisplay==3.0
urdfpy==0.0.22
pysdf==0.1.9
warp-lang==0.10.1
trimesh==3.23.5

# 常用 RL / 科学计算 / 训练可视化工具
pyquaternion
pyyaml
pexpect
matplotlib
einops
tqdm
packaging
h5py
ipython
getkey
chardet
numpy==1.23.2
h5py_cache
opencv-python
scipy
tensorboard
wandb
onnxruntime
mujoco-python-viewer

# 如需特定私有 torch wheel，可将上方 -f URL 替换为企业私有源或自定义 wheel 索引。
